@model IEnumerable<Personal_Management.Models.Work_Schedule>

@{
    ViewBag.Title = "Index";
}

<h2>Графики работы</h2>

<p>
    @Html.ActionLink("Добавить", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naim_Sche)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naim_Sche)
        </td>
        <td>
            @Html.ActionLink("Изменить", "Edit", new { id=item.ID_Schedule }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.ID_Schedule })
        </td>
    </tr>
}

</table>
